# Incluye el sufijo /api (axios usa esta base para /products, /auth, /admin, …)
# Si no levantás el backend local, podés usar directamente el backend desplegado.
VITE_API_BASE_URL=https://carpinteria-backend-production-d5bb.up.railway.app/api
# Para desarrollo full local, cambiá este valor a http://localhost:4000/api
# En Railway: en el servicio del frontend, al hacer build, definí esta variable con la URL pública del backend + /api
