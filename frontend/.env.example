# Incluye el sufijo /api (axios usa esta base para /products, /auth, /admin, …)
VITE_API_BASE_URL=http://localhost:4000/api
# En Railway: en el servicio del frontend, al hacer build, definí esta variable con la URL pública del backend + /api
